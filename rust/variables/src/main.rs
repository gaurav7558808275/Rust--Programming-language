fn main() {
    let mut x = 10; // by default the variable is immutable
    println!("Hello, world! the value is {}",x);
    x = 11;  // mut keyword makes it muttable!
    println!("Hello, world! the value is {}",x);

}
